# Knowledge base

Durable notes the bots should read before inventing a brief.

- `experiments/` — live tests, kill rules, results
- `meetings/` — decisions from rooms and standups
- `decisions/` — locked calls (one file per decision)

Chat is not the source of truth. If it matters next week, write it here.
